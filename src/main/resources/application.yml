quarkus:
  datasource:
    db-kind: mysql
    username: root
    password: admin
    jdbc:
        url: jdbc:mysql://localhost:3306/quarkus_example_db

  hibernate-orm:
    database:
      generation: drop-and-create
